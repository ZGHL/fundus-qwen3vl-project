# Observed working Python package versions in gb10_pytorch_zgh.
# Treat this as a reproducibility record, not a universal pip install file.
# The PyTorch build is NVIDIA container-specific and may need to come from the VM image.
transformers==5.0.0
peft==0.18.1
accelerate==1.11.0
datasets==4.0.0
trl==0.24.0
vllm==0.14.1
opencv-python-headless==4.13.0.92
